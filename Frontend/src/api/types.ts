// import { components } from './schema';


export type DtoGeolocation = {
    clerkId: string,
    latitude: number,
    longitude: number
}

export type DtoUser = {
    username: string,
    clerkId: string
}